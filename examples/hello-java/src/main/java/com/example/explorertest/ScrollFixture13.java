package com.example.explorertest;

// Placeholder — one of 15 files whose only purpose is to make the PROJECT
// pane's file list long enough to overflow its visible height, so its
// scrollbar actually has something to show. See ../../../../../TESTING.md.
class ScrollFixture13 {
}
